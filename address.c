#include<stdio.h>
int main()
{
    int no =21;

    printf("value of no is : %d\n",no);

    printf("size of no is :%lu\n",sizeof(no));

    printf("address of no is:%d\n",&no); //& address is used 

    return 0;
}